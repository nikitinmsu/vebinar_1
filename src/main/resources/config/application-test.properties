# =====================================================================
#  application-test.properties — конфигурация контура TEST (QA-стенд)
# =====================================================================
#  Загружается при:  -Dapp.env=test
#  На тестовом стенде обычно своя БД и отдельные тестовые учётки,
#  чтобы не мешать разработчикам.
# =====================================================================

server.port = 8081
server.timeout.ms = 3000
server.environment = TEST

auth.username = qa_user
auth.password = qa_secret_456
